#include "main.h"
#include <string.h>
/**
 * _strlen - return lenght of string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int i = 0;

	while (i != '\0')
	{
		i++;
		s++;
	}
	return (0);
}
